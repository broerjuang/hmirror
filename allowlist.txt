# Global Allowlist
#
# This file contains domains that should NEVER be blocked, regardless of
# which blocklist includes them. Add one domain per line.
#
# Lines starting with '#' are comments and will be ignored.
# Empty lines are also ignored.
#
# Format: one domain per line (no wildcards, no IP addresses)
# Example:
#   example.com
#   subdomain.example.org
#
# Note: Subdomains are NOT automatically included. If you want to allow
# both example.com and sub.example.com, add both entries.

# Test entries
amazon.ca
amazon.co.jp

